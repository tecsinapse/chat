import { makeStyles } from "@material-ui/styles";

export const useStyle = makeStyles(() => ({
  banner: {
    display: "block",
    marginLeft: "auto",
    marginRight: "auto",
    width: "100%",
  },
}));
