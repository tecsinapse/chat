import { makeStyles } from '@material-ui/styles';

export const useStyle = makeStyles(theme => ({
  blockedMessage: {
    background: '#f2f2f2',
    padding: '5px',
    borderRadius: '10px',
  },
}));
