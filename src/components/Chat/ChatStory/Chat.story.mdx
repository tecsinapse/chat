import { Meta, Story, Props, Source } from '@storybook/addon-docs/blocks';
import { Chat } from './../Chat';
import { ChatStory } from './ChatStory';
import {
  dummyMessagesMedia,
  dummyMessagesTextError,
  dummyChatList,
  dummyGroupedMessages,
} from './dummyMessages';

<Meta title="Chat" component={Chat} />

# Chat

The Chat component can receive the following props:

<Props of={Chat} />

# Code snippets

Here you can check the code snippet for each story.

## Chat Echo

<Story name="Chat Echo" parameters={{ docs: { disable: true } }}>
  <ChatStory isMaximizedOnly />
</Story>

<Source id="chat--chat-echo" />

## Status Text

<Story name="Status Text" parameters={{ docs: { disable: true } }}>
  <ChatStory isMaximizedOnly initialMessages={dummyMessagesTextError} />
</Story>

<Source id="chat--status-text" />

## Status Image

<Story name="Status Image" parameters={{ docs: { disable: true } }}>
  <ChatStory initialMessages={dummyMessagesMedia} isMaximizedOnly />
</Story>

<Source id="chat--status-image" />

## Error Connection

<Story name="Error Connection" parameters={{ docs: { disable: true } }}>
  <ChatStory isMaximizedOnly error="Erro de conexão. Tente mais tarde!" />
</Story>

<Source id="chat--error-connection" />

## Chat Blocked

<Story name="Chat Blocked" parameters={{ docs: { disable: true } }}>
  <ChatStory
    isMaximizedOnly
    isBlocked
    loadingEnabled
    initialMessages={[]}
    blockedMessage="Chat bloqueado"
  />
</Story>

<Source id="chat--chat-blocked" />

## Chat List

<Story name="Chat List" parameters={{ docs: { disable: true } }}>
  <ChatStory
    isMaximizedOnly
    initialMessages={dummyMessagesTextError}
    initialChatList={dummyChatList}
  />
</Story>

<Source id="chat--chat-list" />

## Chat Grouped

<Story name="Chat Grouped" parameters={{ docs: { disable: true } }}>
  <ChatStory
    isMaximizedOnly
    initialMessages={dummyGroupedMessages}
  />
</Story>

<Source id="chat--chat-list" />
